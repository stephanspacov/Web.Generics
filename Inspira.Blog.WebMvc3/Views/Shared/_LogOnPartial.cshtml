@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    [ @Html.ActionLink("Log Off", "LogOff", "Identity") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "Identity") ]
}
